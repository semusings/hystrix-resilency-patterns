
hystrix:
  command:
    default: # Command Key
      execution:
        # Option-1
        timeout:
          enabled: false
        # Option -2
        isolation:
          thread:
            timeoutInMilliseconds: 60000
app:
  aws:
    region: us-east-1
    queue-url: https://sqs.us-east-1.amazonaws.com/346901423380/orders
