hystrix:
  command:
    default: # Command Key
      execution:
        # Option-1
        timeout:
          enabled: false
        # Option -2
        isolation:
          thread:
            timeoutInMilliseconds: 60000
